import { Header } from "../components/header/header";
import { Box } from "@mui/material"

export const AboutPage = () => {
    return(
        <Box>
            <Header/>
            <div>
                Us.
            </div>
        </Box>
    )
}